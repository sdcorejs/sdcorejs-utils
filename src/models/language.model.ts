/**
 * Supported locale codes used by `I18nService` and `SUPPORTED_LANGUAGES`.
 *
 * - `'vi'` — Vietnamese
 * - `'en'` — English
 * - `'ja'` — Japanese
 * - `'ko'` — Korean
 * - `'zh'` — Chinese (Simplified)
 *
 * @example
 * const lang: Language = 'vi';
 * i18nService.setLanguage(lang);
 */
export type Language = 'vi' | 'en' | 'ja' | 'ko' | 'zh';
