/** Default placeholder string used to display empty/null values in UI. */
export const EMPTY_STR = '--';

/**
 * localStorage key used by I18nService to persist the active language.
 * Used by BrowserUtilities to read the current language without injecting I18nService.
 */
export const SD_LANGUAGE_STORAGE_KEY = 'sd-core.language';
